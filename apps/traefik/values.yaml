deployment:
  replicas: 1

service:
  type: LoadBalancer

ports:
  web:
    port: 80
    exposedPort: 80
  websecure:
    port: 443
    exposedPort: 443
  traefik:
    port: 9000
    expose:
      default: true

ingressRoute:
  dashboard:
    enabled: true

logs:
  general:
    level: INFO
